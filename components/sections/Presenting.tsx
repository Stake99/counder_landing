"use client";

/**
 * Scroll spacer for the convergence/resolve beat (≈0.55–1.0 of the journey).
 * The headline copy has been removed — the globe's descent onto Cape Town and
 * the arcs landing carry this moment on their own. The section is kept (height +
 * #presenting anchor) so the scroll journey's timing is unchanged.
 */
export function Presenting() {
  return (
    <section
      id="presenting"
      aria-label="Counder Conference 2027"
      className="relative z-10 min-h-[110svh]"
    />
  );
}
