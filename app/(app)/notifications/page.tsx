/** Notifications — placeholder route. TODO: web push (VAPID) for invites, sessions, and messages */
export default function Page() {
  return (
    <main className="mx-auto max-w-[var(--content-width)] px-[var(--side-padding)] py-24">
      <p className="kicker text-gray-muted">Counder Connect</p>
      <h1 className="mt-4 text-3xl font-medium tracking-tight">Notifications</h1>
      <p className="mt-3 max-w-prose text-gray-body">
        TODO: web push (VAPID) for invites, sessions, and messages
      </p>
    </main>
  );
}
