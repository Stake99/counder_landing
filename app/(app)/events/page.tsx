/** Events — placeholder route. TODO: event management + session scheduling for the Counder Conference */
export default function Page() {
  return (
    <main className="mx-auto max-w-[var(--content-width)] px-[var(--side-padding)] py-24">
      <p className="kicker text-gray-muted">Counder Connect</p>
      <h1 className="mt-4 text-3xl font-medium tracking-tight">Events</h1>
      <p className="mt-3 max-w-prose text-gray-body">
        TODO: event management + session scheduling for the Counder Conference
      </p>
    </main>
  );
}
