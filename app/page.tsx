import { HomeExperience } from "@/components/HomeExperience";

/**
 * Home — the full-site globe experience: one persistent 3D globe background
 * flown through on scroll, with the Counder story (hero → the three pillars →
 * the conference → the invitation) layered over it. See components/HomeExperience.
 */
export default function Home() {
  return <HomeExperience />;
}
